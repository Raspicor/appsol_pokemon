# module.PetApp.open_mega_evolve._refresh_count
# source line 12514
# Recovered from bytecode; default argument values are not shown.

def _refresh_count():
    n = (lambda .0: for None in .0:
    v = Noneif not v.get():
    continue1)(check_vars.values()())
    count_var.set(f'''선택: {n}/{MEGA_SACRIFICE_N}''')
