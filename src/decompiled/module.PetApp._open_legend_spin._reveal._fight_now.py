# module.PetApp._open_legend_spin._reveal._fight_now
# source line 13619
# Recovered from bytecode; default argument values are not shown.

def _fight_now():
    win.destroy()
    self.open_battle(entry, level, legend_token_gen = gen)
