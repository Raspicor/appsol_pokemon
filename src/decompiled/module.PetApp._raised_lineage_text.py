# module.PetApp._raised_lineage_text
# source line 5147
# Recovered from bytecode; default argument values are not shown.

def _raised_lineage_text(self, dex):
    try:
        d = int(dex)
        rec = self.state.get('caught', { }).get(str(d), { })
        if not rec.get('raised'):
            return None
        if not None.get('lineage'):
            None.get('lineage')
        chain = [
            d]
        for None in names,:
            pass
        names, = , []
        cd = chain, cd
        return ' → '.join(names)
    except Exception:
        return None
