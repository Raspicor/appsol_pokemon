# module._region_master_cond
# source line 2244
# Recovered from bytecode; default argument values are not shown.

def _region_master_cond(gen):
    need_stage = gen * 100
    return (lambda state: if gen_dex_complete(state, gen):
    gen_dex_complete(state, gen)if _inf_meter_max_best(state) >= need_stage:
    _inf_meter_max_best(state) >= need_stagegenN_gym_badges_complete(state, gen))
