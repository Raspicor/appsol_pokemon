# module.gym_badges_held
# source line 1963
# Recovered from bytecode; default argument values are not shown.

def gym_badges_held(state):
    out = set()
    if not state.get('gym_badges', []):
        state.get('gym_badges', [])
    for x in []:
        out.add(int(x))
    return out
    except Exception:
        continue
