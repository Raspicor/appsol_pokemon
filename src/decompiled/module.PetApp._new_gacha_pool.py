# module.PetApp._new_gacha_pool
# source line 12265
# Recovered from bytecode; default argument values are not shown.

def _new_gacha_pool(self):
    cells = []
    for None in GACHA_POOL_COMPOSITION:
        amount = ()
        count = None
    None(cells)
    return {
        'revealed': [
            False] * len(cells),
        'cells': cells }
