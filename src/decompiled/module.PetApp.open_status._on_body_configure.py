# module.PetApp.open_status._on_body_configure
# source line 18385
# Recovered from bytecode; default argument values are not shown.

def _on_body_configure(evt):
    try:
        outer_canvas.configure(scrollregion = outer_canvas.bbox('all'))
        return None
    except Exception:
        return None
