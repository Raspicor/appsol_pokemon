# module.PetApp.open_pvp_battle._update_atk_btn
# source line 18082
# Recovered from bytecode; default argument values are not shown.

def _update_atk_btn():
    if not b['ended']:
        not b['ended']
        if b['opp_ready']:
            b['opp_ready']
    ok = b['my_turn']

    try:
        if ok:
        
            try:
                pass
            return None
            except Exception:
                return None
