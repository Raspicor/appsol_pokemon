# module._sort_titles_top_priority._key
# source line 2487
# Recovered from bytecode; default argument values are not shown.

def _key(pair):
    idx = ()
    item = pair
    return (priority.get(tid, len(priority)), idx)
