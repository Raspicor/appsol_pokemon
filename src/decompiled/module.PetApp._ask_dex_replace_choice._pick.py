# module.PetApp._ask_dex_replace_choice._pick
# source line 11273
# Recovered from bytecode; default argument values are not shown.

def _pick(replace):
    result['replace'] = replace

    try:
        win.grab_release()
        win.destroy()
        return None
    except Exception:
        continue
