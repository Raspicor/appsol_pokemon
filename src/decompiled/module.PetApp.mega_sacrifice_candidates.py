# module.PetApp.mega_sacrifice_candidates
# source line 6952
# Recovered from bytecode; default argument values are not shown.

def mega_sacrifice_candidates(self):
    my_element = self.current_element()
    sacrificed = set(self.state.get('sacrificed', []))
    out = []
    for info.get('level', 1) if isinstance(info, dict) else 1 in self.state.get('caught', { }).items():
        dex_s = ()
        info = None
        if d in sacrificed:
            continue
        if lv < MAX_PLAYER_LEVEL:
            continue
        e = POKEDEX.get(d)
        if e or e.get('element') != my_element:
            continue
        out.append(d)
    return out
    except Exception:
        continue
