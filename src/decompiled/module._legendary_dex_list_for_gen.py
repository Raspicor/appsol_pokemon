# module._legendary_dex_list_for_gen
# source line 1431
# Recovered from bytecode; default argument values are not shown.

def _legendary_dex_list_for_gen(gen):
    (lo, hi) = gen_dex_range(gen)
    return (lambda .0: for None in .0:
    d = ()e = Noneif  <= lo, d:
    if not lo, d < hi:
    continueelse:
    .0if not e.get('legendary'):
    continueif not d not in LEGEND_TOKEN_EXCLUDE_DEX:
    continued)(POKEDEX.items()())
