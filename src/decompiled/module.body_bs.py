# module.body_bs
# source line 3052
# Recovered from bytecode; default argument values are not shown.

def body_bs(ctx, key):
    if key == 'body2':
        return ctx.get('body2_bs')
    return None['player_bs']
