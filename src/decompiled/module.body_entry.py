# module.body_entry
# source line 3048
# Recovered from bytecode; default argument values are not shown.

def body_entry(ctx, key):
    if key == 'body2':
        return ctx.get('body2_entry')
    return None['player_entry']
