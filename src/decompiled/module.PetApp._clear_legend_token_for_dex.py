# module.PetApp._clear_legend_token_for_dex
# source line 14771
# Recovered from bytecode; default argument values are not shown.

def _clear_legend_token_for_dex(self, dex):
    try:
        dex = int(dex)
        for gen in (1, 2, 3, 4):
            key = f'''legend_token_gen{gen}'''
            for None in bundles,:
                if not int(b.get('dex', -1)) != dex:
                    continue
            bundles, = , []
            b = self._legend_bundles(gen), b
            if not bundles != self._legend_bundles(gen):
                continue
            self.state[key] = bundles if bundles else None
        return None
    except Exception:
        return None
