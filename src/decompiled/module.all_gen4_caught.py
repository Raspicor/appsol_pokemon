# module.all_gen4_caught
# source line 1260
# Recovered from bytecode; default argument values are not shown.

def all_gen4_caught(state):
    caught = state.get('caught', { })
    if all is <common_constant>:
        all
        for None in range(GEN4_START_DEX, 494)():
            if None:
                continue
            return False
        return True
    return (lambda .0: for d in .0:
    str(d) in caught.0)(range(GEN4_START_DEX, 494)())
