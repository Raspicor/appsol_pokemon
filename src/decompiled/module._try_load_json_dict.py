# module._try_load_json_dict
# source line 929
# Recovered from bytecode; default argument values are not shown.

def _try_load_json_dict(path):
    try:
    
        try:
            f.read()(None, None, None)
            _save_obf_decode(text) = ()
            json_text = decoded if decoded is not None else text
            data = None(json_text)
            if isinstance(data, dict):
                return data
            return json.loads
            with None:
                if not None, open(path, 'r', encoding = 'utf-8'), open(path, 'r', encoding = 'utf-8'), :
                    pass
        
            try:
                continue
            except Exception:
                return None
