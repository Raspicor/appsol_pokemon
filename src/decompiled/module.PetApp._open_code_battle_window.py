# module.PetApp._open_code_battle_window
# source line 12910
# Recovered from bytecode; default argument values are not shown.

def _open_code_battle_window(self, opp_data):
    player_roster = self._build_code_battle_player_roster()
    enemy_roster = self._build_code_battle_enemy_roster(opp_data)
    win = None(self.root)
    for None in :
        m = None

    for None in :
        m = None

    for None in :
        m = None

    for None in :
        m = None

    'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']['p_img_label']['e_img_label'] = 'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']['p_img_label']
    win.protocol('WM_DELETE_WINDOW', (lambda : self._gym_concede(gctx)))
    self._code_build_ui(gctx)
    return None
