# module.PetApp.open_game_throw._resolve
# source line 6422
# Recovered from bytecode; default argument values are not shown.

def _resolve():
    if ctx['resolved']:
        return None
    ctx['resolved'] = None
    None()
    zs = ctx['zone_start']
    if  <= zs, ctx['pos'] or zs, ctx['pos'] <= zs + ctx['zone_w']:
        pass
    else:
        _cancel
    win.after(300, _new_round)
