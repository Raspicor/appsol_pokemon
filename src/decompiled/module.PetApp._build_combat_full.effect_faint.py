# module.PetApp._build_combat_full.effect_faint
# source line 10885
# Recovered from bytecode; default argument values are not shown.

def effect_faint(side):
    info = None(side)
    if info is None:
        return None
    label = ()
    aset = _side_info
    ent = None
    dir_idx = None
    post = None
    target_h = None
    if action is None:
        None()
        return None
    entry_faint_action(aset)(self.root, label, aset, action, dir_idx, target_h, frame_post = post, on_done = render_fn)
