# module.PetApp._gym_effect_faint._done
# source line 17043
# Recovered from bytecode; default argument values are not shown.

def _done():
    self._gym_render_sprites(gctx)
    if on_done:
        None()
        return None
