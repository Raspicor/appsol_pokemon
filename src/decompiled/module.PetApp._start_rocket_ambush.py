# module.PetApp._start_rocket_ambush
# source line 14050
# Recovered from bytecode; default argument values are not shown.

def _start_rocket_ambush(self):
    player_roster = self._build_rocket_player_roster()
    enemy_roster = self._build_rocket_enemy_roster()
    win = None(self.root)

    try:
        win.attributes('-topmost', True)
        for None in :
            m = None
    
        for None in :
            m = None
    
        for None in :
            m = None
    
        for None in :
            m = None
    
        'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']['p_img_label'] = 'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']
        win.protocol('WM_DELETE_WINDOW', (lambda : self._gym_concede(gctx)))
        self._build_rocket_compact(gctx)
        return None
    except Exception:
        continue
