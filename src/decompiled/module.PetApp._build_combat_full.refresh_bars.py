# module.PetApp._build_combat_full.refresh_bars
# source line 10833
# Recovered from bytecode; default argument values are not shown.

def refresh_bars():
    try:
        wild_pb.configure(value = hp['wild'])
        None()
        if refresh_p2_bar:
            None()
            return None
        return refresh_p1_bar
    except Exception:
        continue
