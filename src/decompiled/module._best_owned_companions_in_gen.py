# module._best_owned_companions_in_gen
# source line 1693
# Recovered from bytecode; default argument values are not shown.

def _best_owned_companions_in_gen(state, gen, count):
    lo = ()
    hi = gen_dex_range(gen)
    [] = state.get('caught', { })
    for None in caught.items():
        d_str = ()
        info = None
    None if not  <= lo, d or lo, d < hi else caught.items()
    cands.sort(key = (lambda x: (-x[0], x[1])))
    for None in :
        _ = ()
        d = None

    return d, _
    except Exception:
        continue
    except Exception:
        1 = None
        continue
