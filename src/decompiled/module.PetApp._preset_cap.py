# module.PetApp._preset_cap
# source line 15115
# Recovered from bytecode; default argument values are not shown.

def _preset_cap(self, category):
    if category == 'gym':
        return 2
    if None == 'rocket':
        return ROCKET_PARTY_SIZE
    return None(self.state)
