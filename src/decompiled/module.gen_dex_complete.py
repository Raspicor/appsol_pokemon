# module.gen_dex_complete
# source line 1458
# Recovered from bytecode; default argument values are not shown.

def gen_dex_complete(state, gen):
    if gen == 1:
        return all_gen1_caught(state)
    if None == 2:
        return all_gen2_caught(state)
    if None == 3:
        return all_gen3_caught(state)
    if None == 4:
        return all_gen4_caught(state)
