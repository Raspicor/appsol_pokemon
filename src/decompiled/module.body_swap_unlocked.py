# module.body_swap_unlocked
# source line 891
# Recovered from bytecode; default argument values are not shown.

def body_swap_unlocked(state):
    if state.get('stage', 0) >= 1:
        state.get('stage', 0) >= 1
    return player_level_from_state(state) >= 5
