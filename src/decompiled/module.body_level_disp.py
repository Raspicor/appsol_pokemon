# module.body_level_disp
# source line 3056
# Recovered from bytecode; default argument values are not shown.

def body_level_disp(ctx, key):
    if key == 'body2':
        return ctx.get('body2_level_disp', 1)
    return None['player_level_disp']
