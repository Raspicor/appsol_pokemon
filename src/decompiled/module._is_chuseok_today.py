# module._is_chuseok_today
# source line 2305
# Recovered from bytecode; default argument values are not shown.

def _is_chuseok_today():
    try:
        t = None()
        rng = CHUSEOK_DATE_RANGES.get(t.tm_year)
        if not rng:
            return False
        m1 = ()
        d1 = ()
        m2 = ()
        d2 = time.localtime
        if  <= (m1, d1), today:
             <= (m1, d1), today
            return (m1, d1), today <= (m2, d2)
         <= (m1, d1), today
        return (m1, d1), today
    except Exception:
        return False
