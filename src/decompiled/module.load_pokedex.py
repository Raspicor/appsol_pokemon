# module.load_pokedex
# source line 1117
# Recovered from bytecode; default argument values are not shown.

def load_pokedex():
    try:
    
        try:
            None(f)(None, None, None)
            for None in :
                pass
        
            try:
                return e,
                with None:
                    if not , { }, :
                        pass
                data, e
            
                try:
                    json.load
                    ()
                    None, open(POKEDEX_PATH, 'r', encoding = 'utf-8'), open(POKEDEX_PATH, 'r', encoding = 'utf-8'), 
                    continue
                
                except Exception:
                    ()
                    return 
