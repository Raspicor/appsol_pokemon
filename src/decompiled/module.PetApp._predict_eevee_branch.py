# module.PetApp._predict_eevee_branch
# source line 8131
# Recovered from bytecode; default argument values are not shown.

def _predict_eevee_branch(self):
    if eevee_friendship_ready(self.state):
        hour = None().tm_hour
        if  <= 6, hour or 6, hour < 18:
            return 'psychic'
        return 'dark'
    max(counts, key = (lambda k: counts.get(k, 0))) = None.state.get('element_train', {
        'electric': 0,
        'water': 0,
        'fire': 0 })
    if counts.get(best, 0) == 0:
        best = None([
            'fire',
            'water',
            'electric'])
    return best
