# module.PetApp._toast_corner_geometry
# source line 9443
# Recovered from bytecode; default argument values are not shown.

def _toast_corner_geometry(self, win):
    try:
        win.update_idletasks()
        left = ()
        top = self._rocket_screen_rect()
        area_w = None
        area_h = None
        top + max(0, area_h - h - 60) = left + max(0, area_w - w - 24)
        win.geometry(f'''+{x}+{y}''')
        return None
    except Exception:
        return None
