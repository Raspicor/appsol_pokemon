# module.PetApp._preset_is_customized
# source line 15151
# Recovered from bytecode; default argument values are not shown.

def _preset_is_customized(self, category):
    if any is <common_constant>:
        any
        for None in self._get_preset_slots(category)():
            if not None:
                continue
            return True
        return False
    return (lambda .0: for None in .0:
    d = Noned is not None)(self._get_preset_slots(category)())
