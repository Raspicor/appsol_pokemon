# module.PetApp._build_compact_battle.effect
# source line 10328
# Recovered from bytecode; default argument values are not shown.

def effect(side, crit):
    info = None(side)
    if info is None:
        return None
    label = ()
    aset = _side_info
    ent = None
    dir_idx = None
    post = None
    target_h = None
    play_sprite_action(self.root, label, aset, action, dir_idx, target_h, frame_post = post, on_done = render)
