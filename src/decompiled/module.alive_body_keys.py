# module.alive_body_keys
# source line 3079
# Recovered from bytecode; default argument values are not shown.

def alive_body_keys(ctx):
    keys = [
        'player']
    if ctx.get('has_body2'):
        keys.append('body2')
    for None in :
        if not body_hp(ctx, k) > 0:
            continue
    return k,
