# module.PetApp.open_status._on_status_wheel
# source line 18399
# Recovered from bytecode; default argument values are not shown.

def _on_status_wheel(evt):
    try:
        if evt.delta > 0:
        
            try:
                pass
            return None
            except Exception:
                return None
