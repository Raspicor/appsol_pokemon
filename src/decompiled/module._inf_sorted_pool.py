# module._inf_sorted_pool
# source line 1661
# Recovered from bytecode; default argument values are not shown.

def _inf_sorted_pool(gen):
    lo = ()
    hi = gen_dex_range(gen)
    for None in :
        d = ()
        e = None
        if  <= lo, d:
            if not lo, d < hi:
                continue
            else:
            
            if e.get('legendary'):
                continue

    , [], pool, d = POKEDEX.items(), d, e
    e = None
    pool.sort(key = (lambda d: (_species_strength_score(POKEDEX.get(d, { })), d)))
    return pool
