# module.all_gen3_caught
# source line 1254
# Recovered from bytecode; default argument values are not shown.

def all_gen3_caught(state):
    caught = state.get('caught', { })
    if all is <common_constant>:
        all
        for None in range(GEN3_START_DEX, GEN4_START_DEX)():
            if None:
                continue
            return False
        return True
    return (lambda .0: for d in .0:
    str(d) in caught.0)(range(GEN3_START_DEX, GEN4_START_DEX)())
