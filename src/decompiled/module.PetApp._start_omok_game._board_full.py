# module.PetApp._start_omok_game._board_full
# source line 6785
# Recovered from bytecode; default argument values are not shown.

def _board_full():
    if all is <common_constant>:
        all
        for None in range(n)():
            if None:
                continue
            return False
        return True
    return (lambda .0: for yy in .0:
    for xx in range(n):
    ctx['board'][yy][xx] is not Nonerange(n))(range(n)())
