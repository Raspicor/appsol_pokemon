# module._sort_titles_top_priority
# source line 2482
# Recovered from bytecode; default argument values are not shown.

def _sort_titles_top_priority(items, id_getter):
    for None in :
        i = ()
        tid = None

    tid, i, priority = None, enumerate(TOP_PRIORITY_TITLE_IDS), i, tid, , { }

    def _key(pair):
        idx = ()
        item = pair
        return (priority.get(tid, len(priority)), idx)

    for None in :
        _idx = ()
        item = None

    return item, _idx
