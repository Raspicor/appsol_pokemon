# module.PetApp._make_hscroll_row._bind_wheel
# source line 8209
# Recovered from bytecode; default argument values are not shown.

def _bind_wheel(evt):
    try:
        canvas.bind_all('<MouseWheel>', _on_wheel)
        return None
    except Exception:
        return None
