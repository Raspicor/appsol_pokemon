# module.PetApp._build_compact_battle.refresh_bars
# source line 10290
# Recovered from bytecode; default argument values are not shown.

def refresh_bars():
    try:
        wild_pb.configure(value = ctx['hp']['wild'])
        None()
        if refresh_p2_row:
            None()
            return None
        return refresh_p1_row
    except Exception:
        continue
