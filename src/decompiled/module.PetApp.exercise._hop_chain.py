# module.PetApp.exercise._hop_chain
# source line 11818
# Recovered from bytecode; default argument values are not shown.

def _hop_chain():
    if counter['n'] <= 3:
        self.play_action('Hop', loop = False, on_complete = _hop_chain)
        return None
    None() = None.time
    self.record_daily_action()
    self.save_state()
    self._finish_to_idle()
