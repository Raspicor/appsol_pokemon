# module.PetApp.open_evolution_tab._on_select
# source line 8800
# Recovered from bytecode; default argument values are not shown.

def _on_select(evt):
    sel = listbox.curselection()
    if sel:
        d = ()
        _st = rows[sel[0]]
        if d is not None:
            None(d)
            return None
        return None
