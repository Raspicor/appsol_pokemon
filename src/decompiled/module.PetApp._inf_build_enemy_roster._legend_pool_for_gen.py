# module.PetApp._inf_build_enemy_roster._legend_pool_for_gen
# source line 14335
# Recovered from bytecode; default argument values are not shown.

def _legend_pool_for_gen(g):
    pool = list(LEGENDARY_DEX_BY_GEN.get(g, []))
    if not pool:
        pool = (lambda .0: for None in .0:
    d = ()e = Noneif not e.get('legendary'):
    continued)(POKEDEX.items()())
    return pool
