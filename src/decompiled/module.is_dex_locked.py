# module.is_dex_locked
# source line 1219
# Recovered from bytecode; default argument values are not shown.

def is_dex_locked(state, d):
    try:
        d = int(d)
        return set in (lambda .0: for x in .0:
    if not str(x).lstrip('-').isdigit():
    continueint(x).0)(state.get('party_locked', [])())
    except Exception:
        return False
