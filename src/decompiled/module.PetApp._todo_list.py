# module.PetApp._todo_list
# source line 9483
# Recovered from bytecode; default argument values are not shown.

def _todo_list(self):
    todos = self.state.setdefault('todos', [])
    for None in :
        t = None

    , [], norm, t = todos, t
    self.state['todos'] = norm
    return norm
