# module.PetApp._gym_build_ui._refresh_ult
# source line 17187
# Recovered from bytecode; default argument values are not shown.

def _refresh_ult():
    used = gctx['p_active'] in gctx['p_ult_used']

    try:
        if used:
        
            try:
                pass
            if not used:
            
                try:
                    if gctx['flags']['locked']:
                    
                        try:
                            pass
                        return None
                        except Exception:
                            ult_btn.configure
                            return None
