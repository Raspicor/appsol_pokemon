# module.PetApp.open_evolution_tab._jump_to
# source line 8811
# Recovered from bytecode; default argument values are not shown.

def _jump_to(dd):
    for None in enumerate(rows):
        rd = ()
        _rs = (idx,)
        if not rd == dd:
            continue
        listbox.selection_set(idx)
        listbox.see(idx)
        None(dd)
        _render
        return None
    enumerate(rows)
    if gen_filter['v'] != 'all':
        None('all')
        for None in enumerate(rows):
            rd = ()
            _rs = (idx,)
            if not rd == dd:
                continue
            listbox.selection_set(idx)
            listbox.see(idx)
            None(dd)
            _render
            return None
        enumerate(rows)
    None(dd)
