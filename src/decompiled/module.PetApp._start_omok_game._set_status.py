# module.PetApp._start_omok_game._set_status
# source line 6661
# Recovered from bytecode; default argument values are not shown.

def _set_status(text):
    try:
        status.configure(text = text)
        return None
    except Exception:
        return None
