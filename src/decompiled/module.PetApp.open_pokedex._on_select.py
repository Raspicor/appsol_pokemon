# module.PetApp.open_pokedex._on_select
# source line 16237
# Recovered from bytecode; default argument values are not shown.

def _on_select(evt):
    sel = listbox.curselection()
    if sel:
        None(sel[0])
        return None
