# module.PetApp._build_combat_full.refresh_ult
# source line 10898
# Recovered from bytecode; default argument values are not shown.

def refresh_ult():
    try:
        cur = ctx.get('active', 'player')
        active_entry = body_entry(ctx, cur)
        used = flags['ultimate_used'].get(cur, False)
        if used:
        
            try:
                pass
            return None
            except Exception:
                return None
