# module.PetApp._build_rocket_compact._wheel
# source line 14125
# Recovered from bytecode; default argument values are not shown.

def _wheel(e):
    try:
        if e.delta > 0:
        
            try:
                pass
            return None
            except Exception:
                return None
