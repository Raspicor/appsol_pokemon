# module.get_all_stage_ids
# source line 841
# Recovered from bytecode; default argument values are not shown.

def get_all_stage_ids(species_key):
    sp = SPECIES[species_key]
    for None in :
        s = None

    , [], ids, s = sp['stages'], s
    if sp.get('branching'):
        for None in :
            b = None
    
        sp['branches'].values(), b, , [] += b, = ids
    return ids
