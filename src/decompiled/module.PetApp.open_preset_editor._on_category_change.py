# module.PetApp.open_preset_editor._on_category_change
# source line 15479
# Recovered from bytecode; default argument values are not shown.

def _on_category_change(evt):
    label = cat_var.get()
    for None in PRESET_CATEGORY_LABEL.items():
        c = ()
        lb = None
        if not lb == label:
            continue
    None = None
    None()
    None()
