# module.all_gen1_caught
# source line 1206
# Recovered from bytecode; default argument values are not shown.

def all_gen1_caught(state):
    caught = state.get('caught', { })
    if all is <common_constant>:
        all
        for None in range(1, 152)():
            if None:
                continue
            return False
        return True
    return (lambda .0: for d in .0:
    str(d) in caught.0)(range(1, 152)())
