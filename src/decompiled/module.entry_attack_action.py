# module.entry_attack_action
# source line 3567
# Recovered from bytecode; default argument values are not shown.

def entry_attack_action(entry, aset):
    if not entry:
        entry
    if not { }.get('skill'):
        { }.get('skill')
    name = 'Attack'
    if not aset is not None and aset.has(name):
        if aset.has('Attack'):
            name = 'Attack'
            return name
        if not None:
            pass
        name = { }.get('idle', 'Idle')
    return name
