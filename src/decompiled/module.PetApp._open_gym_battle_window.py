# module.PetApp._open_gym_battle_window
# source line 16932
# Recovered from bytecode; default argument values are not shown.

def _open_gym_battle_window(self, gym_idx, fun_mode):
    g = gym_by_index(gym_idx)
    player_roster = self._build_gym_player_roster()
    enemy_roster = self._build_gym_enemy_roster(gym_idx)
    win = None(self.root)
    for None in :
        m = None

    for None in :
        m = None

    for None in :
        m = None

    for None in :
        m = None

    'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']['p_img_label'] = 'e_hp_max'[enemy_roster, m, , []]['p_active']['e_active']['p_ult_used']['flags']['ui']['aset_cache']
    win.protocol('WM_DELETE_WINDOW', (lambda : self._gym_concede(gctx)))
    self._gym_build_ui(gctx)
    return None
