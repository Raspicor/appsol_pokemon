# module.is_master_title_id
# source line 2018
# Recovered from bytecode; default argument values are not shown.

def is_master_title_id(tid):
    if isinstance(tid, str):
        isinstance(tid, str)
    return tid.endswith('_master')
