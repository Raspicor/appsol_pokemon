# module.PetApp.open_pvp_lobby._reset_buttons
# source line 17823
# Recovered from bytecode; default argument values are not shown.

def _reset_buttons():
    try:
        host_btn.configure(state = 'normal')
        join_btn.configure(state = 'normal')
        return None
    except Exception:
        return None
