# module._save_state_atomic_to
# source line 1075
# Recovered from bytecode; default argument values are not shown.

def _save_state_atomic_to(path, state):
    try:
        d = os.path.dirname(path)
        if d:
        
            try:
                None(d, exist_ok = True)
                tmp = path + '.tmp'
                json_text = None(state, ensure_ascii = False, indent = 2)
                f.write(_save_obf_encode(json_text))
            
                try:
                    ()(None, None, None)
                
                    try:
                        if os.path.exists(path):
                        
                            try:
                                None(path, path + '.bak')
                            
                                try:
                                    None(tmp, path)
                                    return check is not None
                                    with None:
                                        if not _try_load_json_dict(path):
                                            pass
                                    os.replace
                                
                                    try:
                                        shutil.copy2
                                        json.dumps, open(tmp, 'w', encoding = 'utf-8'), open(tmp, 'w', encoding = 'utf-8'), 
                                        os.makedirs
                                        continue
                                        except Exception:
                                        
                                            try:
                                                continue
                                            
                                                try:
                                                    pass
                                                except Exception:
                                                    return False
